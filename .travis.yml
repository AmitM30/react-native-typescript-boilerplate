before_install: nvm install "$(jq -r '.engines.node' package.json)"
# safelist
branches:
  only:
    - master
    - dev
jobs:
  include:
    - stage: Test and lint ✅
      language: node_js
      node_js: 8.11.2
      install: yarn
      script: npm run lint
